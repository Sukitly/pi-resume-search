import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function sessionSearchExtension(_pi: ExtensionAPI): void {
  // Scaffold only. Search and command registration are not implemented yet.
}
